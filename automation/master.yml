---
 - hosts: 127.0.0.1
   connection: local
   remote_user: root
   become: yes
   tasks:
   - name: Base docker and kubernetes installation
     include_role: k8s_server
   - name: Control plane
     include_role: control
