---
 - hosts: 127.0.0.1
   connection: local
   remote_user: root
   become: yes
   tasks:
           - import_tasks: ../shared/k8s_server.yml
           - block:
                   import_tasks: ../control/playbook.yml
             when: "{{ role }}" == 'control'
